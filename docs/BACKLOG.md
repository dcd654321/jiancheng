# 上线前开发清单

更新：2026-09-21。产品：渐成习惯打卡。当前原则：正式数据以云数据库为唯一权威，本机只保留确认快照、离线队列和旧数据导出能力。

## 已完成：产品与本地实现

- [x] 原生今日/进度/我的三Tab及六张81×81透明导航图标。
- [x] 习惯创建、执行日、目标与简化目标、完成/撤销/备注、次日编辑、暂停/恢复/归档。
- [x] 近7/28天统计、历史记录、CSV和原始备份导出。
- [x] 北京日期、跨日前台刷新、旧日期操作保护、存储失败不伪造成功。
- [x] 本机规则计划助手与启动短句；真实AI保持关闭。
- [x] 云端唯一工作区；删除本机/云端来源切换、手动连接和断开。
- [x] 首次数据说明、加载中、断网可读、无缓存读取失败四类状态。
- [x] 云环境隔离的同意与账户摘要绑定；旧开发数据只导出、不上传、不合并。
- [x] 当天记录持久离线队列、单飞自动提交、30秒前台刷新门槛、冲突停止自动请求。
- [x] 管理类操作在线确认、幂等操作ID、版本比较、数据代际和整批冲突处理。
- [x] 云端确认删除；收到有效新代际空快照后才清理本机数据，失败全部保留。
- [x] 2026-09-16本地门禁：147项测试、97项结构检查、云包一致性、Node 16兼容、10个WXML和全部WXSS通过。
- [x] 363×785模拟器发布界面复看：7个正式页面，以及加载、不可用、正常任务3种状态；控制器逻辑由自动化测试覆盖。
- [x] B019评审批次A：首次读取失败重试、备注会话草稿、实时同步状态、前台联网恢复；174项测试、105项结构检查及原生编译通过。证据与回滚见`RELIABILITY-FIX-20260920.md`。

## 已完成：B020评审批次B/C代码与本机模拟器验证

- [x] 创建表单保留主要输入，其余选项收起且摘要可见；星期固定七列，补单位/时间箭头。363px原生模拟器同排48px高，默认保存按钮首屏可见；其他尺寸仍待真机验收。
- [x] 名称按20个Unicode字符统一输入、计数及校验；范围提示和字段错误就地显示；保存时锁定输入，失败保留原值。
- [x] 压缩首页顶部、突出任务和完成动作；完成组可展开、撤销独立；统计突出实际完成/计划次数，日历补文字状态和四类图例。
- [x] 我的页按管理/偏好/数据隐私/帮助分组；数据二级页集中导出与找回，删除保留双确认并防重复提交、处理清理失败与页面卸载。
- [x] 详情备注与历史层级精简，只显示最近有安排的日期，默认7条；更多管理动作折叠，备注草稿保护不变。
- [x] 计划助手只先问方向与分钟，频率按需展开；基础建议如实标识，采用后预填确认表单，未保存不创建。
- [x] 管理、备份和数据页完善读取中/不可用分支；微信内置反馈入口已接入，未在真机提交反馈。
- [x] 新增13项回归，合计187项通过；113项结构检查、云包一致、11个WXML及全部WXSS原生编译通过；16张363×785截图已查看。证据和回滚见`UX-FIX-20260920.md`。
- [x] 同步时间由原始UTC字符串改为明确标注的北京时间。

## 下一步：真实云闭环

- [x] B021本地资源专属命名：`jiancheng_daka_api`、`jiancheng_daka_accounts`、预留`jiancheng_daka_plan`；脚本/SDK测试/锁文件/调用默认值同步，194项测试通过。
- [x] 正式目标预备配置默认关闭；新增正式数据命名空间，不读旧测试绑定、快照与队列；旧测试运行目标明确保留。
- [ ] 提供/核实`product`完整环境ID；如由另一小程序共享，核实资源方AppID、授权及可信身份。当前工具仅返回旧已知ID，未识别product目标。
- [ ] 只部署`jiancheng_daka_api`、创建并封闭客户端直读写`jiancheng_daka_accounts`，验证实际入口后切换活动配置。禁止覆盖其他小程序资源或自动迁移旧测试数据。见`CLOUD-NAMESPACE-20260921.md`。

以下有日期的已完成项仅对应旧测试云；不代表新的product已配置或验收。

- [x] 2026-09-16只读复核 `cloud1-d4gq76oyt363f08a7`、`habitApi`、Nodejs16.13、`yidian_accounts` 空集合和2个索引；函数短暂显示Updating后复查为Active。
- [ ] 处理或书面接受 `wx-server-sdk 4.0.2` 上游依赖审计风险；不得用强制 `audit fix` 代替兼容性验证。
- [x] 用户确认后在测试云 `habitApi` 配置服务端 `HABIT_APP_ID`、`HABIT_MINIPROGRAM_ONLY=true`、`HABIT_API_ENABLED=true`；重新打开配置页验证持久化。
- [x] 运行时授权已批准；实际小程序身份通过校验，云控制台测试被拒绝。诊断确认平台附带`userInfo`/`tcbContext`触发业务白名单误拒，本地修复及152项测试通过。
- [x] 最终入口单文件更新已成功，实际探针确认正式响应且无诊断字段。
- [x] B018启用测试云并修复冷启动刷新；159项测试、100项结构检查、云包一致性与原生编译通过。真实创建、打卡自动提交、确认缓存移除后恢复及进度统计通过，见`STARTUP-FIX-20260920.md`。
- [ ] 在开发者本人测试账户验证首次 `pull`、创建、编辑、打卡、备注、自动提交、刷新、导出和 `purge`。
- [ ] 验证云函数真实入口中的 APPID、OPENID、SOURCE；客户端直接读写集合必须继续被拒绝。
- [ ] 两账户隔离、两设备版本冲突、断网重连、响应丢失、跨日、旧设备删除防回传。
- [ ] 验证事务首次建档、竞争、回滚、超时和未知SDK错误不写入假空账户。
- [ ] 完成服务端每用户/全局限流、费用阈值、预算告警、日志脱敏与回滚操作说明。
- [x] `miniprogram/config/cloud.js` 已设为 `enabled: true` 供开发者受控联调；双账户、真机与安全隐私验收仍是公开发布门槛。

## 真机与发布

- [ ] iOS和Android真机完整走一遍：首次同意 → 创建 → 打卡 → 简化 → 统计 → 编辑 → 导出 → 删除。
- [ ] 验证320/375/430宽度、大字号、键盘遮挡、安全区、弱网、冷启动和后台恢复。
- [ ] 真机验证CSV/JSON写入、重命名、发送及清除与在途导出的竞态。
- [ ] 补正式隐私说明、删除后最小元数据的用途与保留期限；真机验证已接入的微信反馈，并确定正式客服方式。
- [ ] 核验微信公众平台名称、头像、类目、用户隐私保护指引、备案、审核和2026赛事资格。
- [ ] 发布前检查云环境配额、账单提醒和数据备份策略；先小范围测试，再决定是否开放公众。

## AI增强（后续版本）

- [x] 本机规则建议、预览和带入创建表单；模型关闭或异常时不会冒充AI。
- [ ] 选择可在中国境内稳定服务的模型，凭据仅放云端环境变量。
- [ ] 服务端输入限制、内容安全、结构化输出校验、同请求缓存、日/月成功额度和总预算熔断。
- [ ] 先限定为“生成或调整一个习惯计划”，保留用户对执行日、时间和目标的最终控制。
- [ ] 明确AI标识与隐私告知；模型不可用时回退到本机规则建议。

本阶段不做积分、分享奖励、支付、广告、发现页、书籍推荐或内容社区，避免扩大开发和运维范围。
