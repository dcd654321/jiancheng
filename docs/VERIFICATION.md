# 发布准备验证记录

更新日期：2026-09-21。产品：渐成习惯打卡。工程：`D:\codex\coding\yidian-miniprogram`。

结论：旧测试云及基础云业务可用；B019可靠性、B020页面整改和B021本地资源专属命名均已实现。当前194项测试、116项结构检查、云包一致性、Node 16及原生编译通过。新的product尚未识别实际ID、部署或切换；完整业务、双账户、真机、安全隐私和平台审核尚未完成，不能宣称正式上线就绪。最新证据及回滚见[CLOUD-NAMESPACE-20260921.md](CLOUD-NAMESPACE-20260921.md)，页面见[UX-FIX-20260920.md](UX-FIX-20260920.md)，可靠性见[RELIABILITY-FIX-20260920.md](RELIABILITY-FIX-20260920.md)，旧基础云流程见[STARTUP-FIX-20260920.md](STARTUP-FIX-20260920.md)。

## 2026-09-21 B021共享云资源前缀

- 本地云函数目录改为`jiancheng_daka_api`，仓库集合固定`jiancheng_daka_accounts`，AI预留名`jiancheng_daka_plan`；更新构建、Node16、SDK合同测试、包名与锁文件。只移动本应用目录，其他小程序目录未操作。
- 新增7项回归：统一命名、部署目标预备/旧测试保留、明确函数调用且不回退、非法函数名拒绝、同环境绑定隔离、同环境旧快照及队列不继承、空product配置不联网。原187项仍通过，合计194项、0失败/跳过。
- 116项结构/语法/页面检查；5个共享云模块与源码一致；Node16.13.2下6个云包JS语法、真实SDK加载及randomUUID通过；11个WXML/全部WXSS编译通过，界面源码未修改。
- npx离线Node16检查首次因全局npm缓存访问权限失败，取得该离线检查命令的单次权限后成功；无新在线依赖安装、云部署或配置变更。
- 官方CLI两次只读环境列表仅返回`cloud1-d4gq76oyt363f08a7`，没有可确认的product环境ID；浏览器控制通道查询失败，不绕过登录或读取认证文件。未把显示名当环境ID使用。
- 原生模拟器只读检查为`pages/today/index`、ready=true、pending=0、count=1；活动配置仍是旧测试`habitApi`。没有运行product请求，没有创建/改名/删除集合或函数、迁移业务数据、修改规则和共享授权。
- 新代码验证通过不等于新环境已部署；跨AppID共享初始化和身份语义尚需资源方信息核验，绝不为连通而放宽身份校验。历史上线门槛仍保留。

## 2026-09-21 B020页面、表单与信息层级整改

- 已实施创建精简/七列星期/就地校验、首页任务优先、详情备注与历史折叠、统计次数优先和文本图例、我的分组及数据二级页、诚实的备份说明、计划助手两阶段确认。保留既有云数据权威及同步保护。
- 13项新增页面回归，加上原174项，187项全通过、0失败/跳过；113项结构检查、5个云共享模块一致；11个WXML输入编译为122203字节，全部WXSS通过，app.wxss产物9327字节。云包代码未修改，本轮未重复Node 16专用检查。
- 新证据`evidence-b020/`共16张PNG，逐张查看；均为微信开发者工具实际363×785模拟器，不是网页复刻。08与09为同页滚动尝试前后，因整页可见而重复，不计两个不同布局。
- 原生布局测量：7个星期同一行、每个高48px；默认创建保存按钮底部666.05px，内容视口高671px，首屏可见。真实输入21个emoji后保留20个、计数20。未测试其他屏幕或键盘弹起。
- 实际助手生成规则预览，点击采用后带入名称/目标/频率等已有值；未点击保存。部分路由通过CLI调用页面已有导航方法，不能声称全部是物理点击。截图覆盖今日、创建/展开、详情/更多、进度、我的、数据、备份、助手/预览/采用、同步、管理和编辑。
- 最后真实会话ready=true、pending=0、仍1个原有测试习惯，业务state与验收前JSON完全一致，临时内存比对属性已移除；已回到今日。控制台按error/warn/fail筛选未命中，不代表覆盖所有潜在运行错误。
- 未创建、编辑、打卡、撤销、删除或导出真实业务数据，未提交反馈；没有部署云函数、修改云配置/权限、清理业务缓存或提交审核。删除、导出异常与页面卸载防护本轮用内存夹具回归，非真实云删除验收。
- DevTools退出后通过既有CLI重开。启动时沙箱无法写工具自己的User Data/.cli，获得这一次启动命令的权限后恢复；沿用既有automation授权，不修改项目配置或关闭安全校验。
- 尚未完成：320/375/430、大字号、键盘、安全区、iOS/Android真机、物理弱网/双设备、正式反馈和完整云业务闭环。范围B/C的代码整改完成，跨设备验收门槛仍保留。

## 2026-09-20 B019可靠性修复

- 15项新增回归：首次失败后实际同步页重试、备注刷新/重开/存储失败/跨日/账户切换保护、通知生命周期、离线文案与时间格式、慢请求期间入队、操作ID复用、联网事件去抖与无自循环、表单输入及错误保留。
- 全套174项通过，0失败、0跳过；105项结构检查、5个共享云模块一致；10个WXML输入产物101069字节、全部WXSS通过。
- 真实363×785模拟器：正常云启动；现有合成习惯输入未保存备注、刷新、返回再打开，草稿保留且未入队；清除该验收草稿后，业务state与测试前完全一致。
- 离线截图通过调用会话网络状态入口注入，仅证明页面通知与渲染；随后恢复入口真实读取云端，当前同步页自动恢复“数据已同步”，时间为北京时间。未修改系统网络，因此不算物理弱网/断网重连验收。
- 新截图`evidence-b019-20260920/`共4张，均已查看；未重新部署云函数、未修改云配置或数据库权限、未新增/编辑/删除真实云业务数据。
- 先前`audits/20260920-ux/`保留修复前评审及故障复现历史；其中旧探针断言的是故障行为，不作为修复后的回归命令。

## 2026-09-16本地门禁

| 检查 | 结果 |
| --- | --- |
| Node.js测试 | Node 24.19.0，147项通过，0失败、0跳过 |
| 结构/语法/页面检查 | 97项通过 |
| 云函数生成物 | 5个共享模块与服务端源码一致 |
| Node 16兼容 | Node 16.13.2；6个云包文件语法、`wx-server-sdk`加载、`crypto.randomUUID`通过 |
| WXML | 微信开发者工具 `wcc` 编译10个输入，产物99902字节 |
| WXSS | 全部样式输入通过微信开发者工具编译器 |
| 客户端云配置 | `envId=cloud1-d4gq76oyt363f08a7`，`enabled:false` |

测试覆盖包括：

- 北京日期、跨日、执行日、创建上限、完成/撤销、简化/恢复、备注、次日版本、暂停/归档、统计、CSV转义与公式注入。
- 存储失败、坏数据、导出落盘与清除竞态、页面离开和上下文变化。
- 可信身份、字段白名单、原型键、请求大小、事务原子性、并发版本、幂等回执、容量和7天离线窗口。
- 一次性同意、账户绑定、无缓存读取失败不造空数据、已有缓存离线可读、应用生命周期统一捕获失败。
- 当天记录持久队列、单飞自动提交、飞行中追加操作、30秒前台门槛、冲突停止自动请求、慢读取不回滚新确认。
- 正式工作区只读云端；没有数据来源切换、连接/断开和本机恢复覆盖；旧开发数据只导出。
- 管理操作在线确认、响应丢失后复用操作ID、跨日拒绝、保存确认前不导航。
- 删除确认字符串、无队列/无冲突门槛、网络失败全保留、伪确认拒绝、有效新代际空快照后再清理本机数据。

这些测试使用内存仓库、模拟微信传输、模拟存储和页面控制器夹具。SDK合同测试会加载真实适配器代码，但封锁网络并模拟底层响应；它们不是云端E2E。

## 原生模拟器证据

微信开发者工具实际返回的视口为363×785。2026-09-16已打开并截取：

- `evidence-release-today-363-20260916.png`
- `evidence-release-progress-363-20260916.png`
- `evidence-release-mine-363-20260916.png`
- `evidence-release-sync-363-20260916.png`
- `evidence-release-backup-363-20260916.png`
- `evidence-release-edit-363-20260916.png`
- `evidence-release-detail-363-20260916.png`

另用开发者工具运行时 `setData` 仅注入页面显示状态，截取加载、无缓存读取失败和正常任务三种视觉分支：

- `evidence-release-loading-363-20260916.png`
- `evidence-release-unavailable-363-20260916.png`
- `evidence-release-ready-task-363-20260916.png`

状态注入只用于布局和文案复核，不是网络或控制器功能证据；相应逻辑由自动化测试覆盖。正式页面保持白底、深绿主色、20px页边距、开放列表、无阴影卡片堆叠和线性Tab图标。首屏无开发模式、数据来源切换或本机存储警告。

七个正式页面截图完成时，模拟器控制台的 `error`、`warn`、`fail` 和网络记录均为空。之后的状态注入没有写入云端或持久业务数据；重新打开/编译页面即可回到真实启动状态。

此前2026-09-15的390px九页截图和Tab图标截图仍作为历史证据保留，不能替代本次363px正式界面，也不能外推到所有尺寸和真机。

## 外部云只读证据

2026-09-16通过微信开发者工具官方只读接口重新确认：

- AppID `wx58e61dffcbfa4249` 只列出目标环境 `cloud1-d4gq76oyt363f08a7`。
- `habitApi`首次查询为`Updating`，复查稳定为`Active`，运行时Nodejs16.13、超时3秒。
- `yidian_accounts`为0条、0字节、2个索引：`_id_`与`_openid_1`，均无访问计数。
- 本次只查询环境、函数状态和集合结构；没有读取集合文档、修改权限/环境变量或写入数据。

2026-09-15的补充历史证据：

- 测试环境和 `yidian_accounts` 集合存在，集合为空并有两个索引。
- 客户端直接读取集合返回 `Permission denied`。
- `habitApi` 为Active、Nodejs16.13；完整部署漏传嵌套 `lib` 后已通过增量部署补齐。
- 调用不存在的无写入探针动作在入口加载后返回 `NOT_ENABLED`，证明当时函数可加载且保护开关关闭。

2026-09-19，用户在明确获知目标与三项值后委托直接操作。微信云开发控制台 `cloud1-d4gq76oyt363f08a7` 的 `habitApi/$LATEST` 已配置 `HABIT_APP_ID=wx58e61dffcbfa4249`、`HABIT_MINIPROGRAM_ONLY=true`、`HABIT_API_ENABLED=true`；保存后重新打开“配置→高级配置”，三项均持久显示，原内存256 MB、超时3秒未变。官方只读接口复查函数为Active、Nodejs16.13、超时3秒；`yidian_accounts`保留2个索引，文档查询返回`total: 0`、`data: []`。未修改数据库权限。

拟从模拟器调用不存在的`deployment_probe`动作以验证服务端身份入口；源码中未知动作在`validateEvent`处拒绝，早于事务仓库，故该探针本身不写数据库。但微信开发者工具将`automation_evaluate`置为`Waiting for user authorization`，轮询仍为`pending`；调用未执行，不能据此声称SOURCE/APPID已通过或被拒绝。没有改用真实`pull`，因为首次`pull`可能创建空账户文档。客户端仍是`enabled:false`。此前2026-09-16本地测试结果未在本次重跑。

回退服务端开关需在测试环境 `habitApi` 的配置中将 `HABIT_API_ENABLED` 改为 `false` 并重新打开核对；文件检查点不回退云端配置。文档检查点为`D:\codex\coding\yidian-recovery\20260919-B016-T8-server-flags\project`。

## 尚未完成

2026-09-20最新进展：最终部署任务`cc2821e1-3e79-4a96-920c-fa0a7164a304`已成功；实际未知动作返回“不支持的请求”，无diagnostic字段。B018真实云账户revision=2、1个合成习惯和1条完成记录；移除确认缓存再重启后业务state完整恢复，首页无需手动刷新。客户端直接读集合仍被拒绝。新截图通道授权超时，本轮不计新增视觉或真机验收。此前日期段落保留历史状态。

- 首次pull/建档、创建/打卡/自动提交/确认缓存恢复已通过；真实编辑/备注、网络失败后的自动重试、冲突、导出和purge仍待验证。
- 双微信账户隔离、双设备并发、跨日、弱网、丢响应和删除后旧设备回传。
- `wx-server-sdk 4.0.2` 上游依赖告警处置、服务端限流、预算告警、日志脱敏和灾难回滚。
- 320/375/430宽度、大字号、键盘遮挡、Android/iOS真机、冷启动和后台恢复。
- 真机CSV/JSON写入、重命名、发送与清除竞态。
- 正式隐私说明、删除元数据保留期限、反馈入口、类目、备案、名称/头像审核和赛事资格。
- 真实AI服务、内容安全、额度和费用保护。当前只有本机规则建议。

## 发布判断

当前可以继续做开发者账户的受控云联调，不能邀请普通用户长期保存重要数据，也不能上传正式版。只有真实云闭环、真机、隐私和平台门禁全部通过后，才能将状态改为“可提交审核”。
