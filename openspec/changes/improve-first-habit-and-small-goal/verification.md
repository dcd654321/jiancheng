# 验证记录（2026-09-23）

## 范围与回滚

- 分支：`dev`；实施前基线：`454a406a086e772e2b7eb726058d48023d80c2d0`。
- 本次提交只含此 OpenSpec 变更、客户端表单/今日页/任务模板/样式和测试。回滚时可在 `dev` 对本次提交执行 `git revert <本次提交>`，不触碰云端数据。
- `docs/DESIGN-*.md`、`docs/PRODUCT-OPTIMIZATION-20260923.md`、`docs/design-diagrams.html` 等实施前已有未跟踪文件未纳入提交。
- 未修改 `server/`、`cloudfunctions/`、集合名称、云端协议或小程序云环境配置；未上传、部署或迁移正式环境。

## 自动验证

- `npm test`：204/204 通过。覆盖双档目标、简化与完成分步、无预设、目标为 1、首次创建今天/明天安排和既有云端队列行为。
- `npm run check`：118 项通过。
- `npm run check:cloud`：5 个云模块与源文件一致；这是本地一致性检查，不是云端部署测试。
- `npm run openspec -- validate --all --strict`：2 个变更通过。
- `git diff --check`：通过；Git 仅提示工作树 LF/CRLF 转换，没有空白错误。

## 微信开发者工具原生模拟器

- 在已打开的微信开发者工具中观察 iPhone 12/13（390px）与 iPhone 5（320px）模拟器的今日页和创建页。创建页主表单能看到忙时目标；今日页显示“平时 / 今天按”和单独的“今天少做一点”“打卡”入口。
- 320px 下发现一行七个星期按钮偏窄，已改为 4+3 布局并在原生模拟器复查，按钮及文字未溢出；创建页底部保存按钮在滚动后可见。开发者工具改动热重载后重新进入页面检查。
- 本轮截图仅通过工具即时显示供检查，未另存本地截图文件。没有在模拟器点击现有习惯的简化、完成、撤销，也没有创建测试习惯，以免改写现有云端测试记录；这些行为只有自动测试证据，原生交互验证未完成。
- 切换模拟器机型时开发者工具出现过“调试器加载错误，从任务栏打开工具可能导致该问题”的工具层提示；随后页面继续渲染，已恢复原来的 iPhone 12/13（390px）机型与今日页。此提示不计为应用测试通过或失败。
- 真机大字号、触控和首次使用的非开发者体验尚未验收。模拟器观察不能代替真机和正式环境验收。
