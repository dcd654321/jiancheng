# Tasks

## 1. 页面实施

- [x] 1.1 将忙时目标移到创建/编辑主表单，更新更多设置摘要和字段错误定位；用表单测试验证有效、空值及非法值。
- [x] 1.2 更新今日任务双档目标、当前目标及完成状态文案，保留单独的简化与完成操作；用页面测试验证原目标、小目标、无预设及目标为 1。
- [x] 1.3 实现首次创建后的真实下一步提示及今日新任务优先可见；用页面测试验证今天执行与今天不执行。
- [x] 1.4 补齐第一次完成的非阻断反馈和详情/进度的状态称谓；用本机与待同步页面测试验证，不改变服务端状态值。

## 2. 验证与交付

- [x] 2.1 运行 `npm test`、`npm run check`、`npm run check:cloud` 和 OpenSpec strict 验证，保存结果；自动测试不得冒充云端部署验收。
- [ ] 2.2 在微信开发者工具原生模拟器检查创建、今日、简化、完成与小屏折行，记录截图或明确记录未执行原因。
- [x] 2.3 核对本次没有云函数/集合/协议变更与正式环境部署，记录 `dev` 提交和回滚点；如需云端实测另立任务。
- [ ] 2.4 由用户在真机检查大字号、触控与首次使用路径，再决定是否合并 `main`；记录验收结果，不以模拟器替代。
