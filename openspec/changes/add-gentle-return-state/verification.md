# 验证记录（2026-09-24）

## 范围和回滚

- 整改项：P0-B / R03「漏做后温和回归」。只修改小程序客户端、自动测试和本 OpenSpec 记录。
- 开发基线：`dev` 的 `41097bad74659f3c54d2dbd05fd91cdc864894d7`。本批从该回滚点开始；需要撤回时在 `dev` 按逆序对本批提交执行 `git revert`，不重置或删除其他改动。
- 无云函数、集合名、服务端协议、正式环境配置变更；没有上传、部署或清理任何正式云资源，也没有写入模拟器里的用户记录。
- 仓库中原有未跟踪的 `docs/DESIGN-*`、`docs/PRODUCT-OPTIMIZATION-20260923.md` 和 `docs/design-diagrams.html` 不属于本批，不纳入提交。

## 自动检查

| 检查 | 结果 |
| --- | --- |
| `npm test` | 219/219 通过，含 14 项新增回归判定/页面测试；均为本地模拟，不是云端验收。 |
| `npm run check` | 121 项语法、配置与页面静态检查通过。 |
| `npm run check:cloud` | 5 个云模块与源码一致；未安装或部署。 |
| `npm run openspec -- validate --all --strict` | 3 个 change 通过、0 失败。 |
| `git diff --check` | 通过，仅有 Windows 行尾转换提示。 |

## 原生模拟器只读观察

- 微信开发者工具 Stable 2.02.2608060，模拟器约 390 × 753 像素。
- 当前账号中已有一项习惯符合此前两个计划日未记录、今天待完成的条件；今日页实际出现一张温和回归卡。两按钮在窄屏下可见且未遮挡；没有把过去自动补记。记录不保存该账号的习惯标题或原始数据。
- 点击“少做一点”打开原有“今天少做一点”输入确认框，选择“取消”；未提交目标修改。
- 点击“按原目标继续”进入该习惯详情；详情中今天仍是原目标、仍“未记录”；返回今日后回归提示重新出现。没有点“打卡”。
- 控制台有开发者工具的预加载、自动热重载及 `reportRealtimeAction:fail not support` 警告；本次观察未见红色运行错误。上述操作只验证模拟器的显示与只读导航，不证明真机、弱网或正式云行为。

## 尚待验收

- OpenSpec 2.4：用户在独立测试账号与真机验证跨日、弱网、触控、文案和真实云同步状态；未取得该证据前不合并 `main`，不宣称上线完成。
- 前批 P0-A 的真机点击及用户独立验收仍未完成，本批检查不替代前批验收。
