# Tasks

## 1. 实施

- [x] 1.1 实现只读回归判定；用纯函数测试覆盖连续计划日、休息日、完成中断、首次生效、暂停/归档、计划变更和跨日。
- [x] 1.2 在今日页展示最多一个回归入口并复用原目标、简化和恢复操作；用页面测试验证目标为 1、无预设小目标、今天无任务、完成后消失且不补写历史。
- [x] 1.3 在待同步/冲突/不可读状态隐藏回归提示，并给回归完成非阻断反馈；用页面测试验证账户隔离、待同步优先与操作失败不报成功。

## 2. 验证与交付

- [x] 2.1 运行 `npm test`、`npm run check`、`npm run check:cloud`、OpenSpec strict 及 `git diff --check`，记录结果；不把模拟测试写成云端验收。
- [x] 2.2 用微信开发者工具原生模拟器只读检查今日页提示在窄屏下的折行、按钮和导航；若缺合适测试数据，明确记录未做实际点击。
- [x] 2.3 核对没有云函数/集合/协议变更或正式部署，记录 `dev` 提交与回滚点；正式云验证另行保留未完成。
- [ ] 2.4 用户在独立测试账号和真机验证跨日、弱网、触控与回归文案后确认；未取得证据不合并 `main`。
