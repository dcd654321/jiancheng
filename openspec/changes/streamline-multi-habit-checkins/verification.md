# Verification

日期：2026-09-24；分支：`dev`。

## 已验证

- `npm test`：226/226 通过；新增原子完成、无忙时目标、已调整目标、已完成、跨日、五项计数、其他账户隔离、注入字段拒绝、同操作号幂等、离线重放与跨设备冲突测试。
- `npm run build:cloud`：本机生成 5 个云模块；没有安装依赖、上传或网络请求。
- `npm run check`：122 项语法／配置／页面结构检查通过。
- `npm run check:cloud`：5 个生成云模块与源代码一致。
- `npm run openspec -- validate --all --strict`：4 个变更规范通过。
- `git diff --check`：通过；Git 仅报告本机换行符将由 LF 转为 CRLF 的提示。
- 微信开发者工具 Stable 2.02.2608060 的 iPhone 12/13 模拟器：实际打开“今日”看到“待做 1 · 已做 0”且首个任务可见；“我的”实际显示“意见与问题反馈”。当前账号的唯一习惯未设忙时目标，因此此处**没有**验证快捷按钮真实渲染或点击。

## 未验证与发布边界

- 没有上传云函数，正式共享云配置仍关闭。旧版云函数不认识 `completeMinimum`；未同时更新客户端和云函数前不能把快捷打卡作为正式可用功能。
- 没有在独立测试账号或真机验证快捷按钮、弱网、系统大字号、微信反馈提交及开发者后台接收，也没有做正式云双账号测试。
- 没有新建集合、迁移数据、清理资源、合并或推送 `main`。此前未跟踪的设计文档未被本批改动。

## 回滚

本批在 `dev` 形成独立提交后，可从该提交逆向恢复本批 UI／命令／生成云模块。回滚不需要改动主账户数据结构；已有忙时完成记录仍满足旧 schema。若以后先部署了云函数再回滚客户端，旧客户端不会发送新命令；若先回滚云函数，应同步移除客户端快捷入口，避免请求被拒绝。
